<div class="container">
    <div class="row justify-content-center" ">
        <div class="col-4 teamInfo ">
            {{#each myTeam}}
            <h1>{{this.team_name}}</h1>
            <h1>{{this.team_sport}}</h1>
            <h1>Wins: {{this.wins}}</h1>
            <h1>Losses{{this.losses}}</h1>
            
           

            {{/each}}

        </div>
           <div class="col-10 teamInfo ">
            {{#each myPlayers}}
            <h1>{{this.first_name}}</h1>
           

            {{/each}}
            {{#each myTeam}}
             <form action=" /teams/{{this.id}} " method="GET ">
        <button type="submit ">Add Player</button>
        </form>
        {{/each}}


    </div>

</div>


</div>